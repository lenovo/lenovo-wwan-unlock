#!/bin/sh

set -e

. /usr/share/debconf/confmodule

case "$1" in
    remove|purge)
        systemctl --system daemon-reload || true
        deb-systemd-invoke restart ModemManager.service || true
    ;;
esac

#DEBHELPER#
